function User() {

	var obj = {};

	//------------
	// Events
	//------------

	obj.addEvent = function (adderss, title, desc, category, img) {
		// todo
	};

	// function removeEvent() ??

	obj.searchEvent = function (address, radius) {
		// todo
	};

	//------------
	// Settings
	//------------

	obj.editSettings = function (favAddress, favCategory, favRadius) {
		// todo
	};

	return obj;

}